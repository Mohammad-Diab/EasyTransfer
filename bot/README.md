# EasyTransfer Telegram Bot

بوت تيليجرام لإدارة طلبات التحويل وجهات الاتصال.

## المميزات

- ✉️ إرسال طلبات تحويل جديدة
- 📦 التحقق من حالة الطلبات
- 📋 عرض الفئات المتاحة
- 👥 إدارة جهات الاتصال (إضافة، حذف، عرض)
- 🔐 نظام صلاحيات للمستخدمين المصرح لهم

## التثبيت

1. **تثبيت المتطلبات:**
```bash
pip install -r requirements.txt
```

2. **إعداد ملف البيئة:**
```bash
cp .env.example .env
```

3. **تعديل ملف `.env`:**
```env
# توكن البوت من @BotFather
BOT_TOKEN=your_bot_token_here

# معرفات المستخدمين المصرح لهم (مفصولة بفواصل)
AUTHORIZED_USERS=123456789,987654321

# ربط معرفات تيليجرام بمعرفات الحسابات في API
# الصيغة: telegram_id:account_id,telegram_id:account_id
USER_ACCOUNT_MAP=123456789:1,987654321:2

# رابط السيرفر
SERVER_URL=http://127.0.0.1:5000
```

## كيفية الحصول على معرف المستخدم في تيليجرام

1. ابحث عن بوت `@userinfobot` في تيليجرام
2. أرسل له `/start`
3. سيرسل لك معرفك (User ID)

## تشغيل البوت

```bash
python main.py
```

## الأوامر المتاحة

- `/start` - بدء البوت وعرض القائمة الرئيسية
- `/send` - إرسال طلب جديد
- `/status` - التحقق من حالة الطلب
- `/status <request_id>` - التحقق من حالة طلب محدد
- `/tiers` - عرض الفئات المتاحة
- `/contact_add` - إضافة جهة اتصال جديدة
- `/contact_delete` - حذف جهة اتصال
- `/contacts_get` - عرض قائمة جهات الاتصال
- `/cancel` - إلغاء العملية الحالية

## API Endpoints المطلوبة

يجب أن يوفر السيرفر الـ endpoints التالية:

### Requests
- `GET /requests/{account_id}/{request_id}` - الحصول على حالة الطلب
- `POST /orders/{account_id}` - إنشاء طلب جديد
  ```json
  {
    "tier": 45,
    "username": "اسم المستخدم",
    "phone": "1234567890"
  }
  ```

### Contacts
- `GET /contacts/{account_id}` - الحصول على قائمة جهات الاتصال
- `POST /contacts/{account_id}` - إضافة جهة اتصال جديدة
  ```json
  {
    "phone_number": "1234567890",
    "name": "اسم جهة الاتصال"
  }
  ```
- `DELETE /contacts/{account_id}/{contact_id}` - حذف جهة اتصال

## معالجة الأخطاء

البوت يتعامل مع الأخطاء التالية:
- **404** - الطلب أو جهة الاتصال غير موجودة
- **400** - بيانات غير صحيحة
- **500** - خطأ من السيرفر
- **Timeout** - انتهت مهلة الاتصال
- **ConnectionError** - فشل الاتصال بالسيرفر

## الهيكل

```
bot/
├── main.py                 # نقطة البداية
├── config.py              # الإعدادات والرسائل
├── handlers/
│   ├── __init__.py
│   ├── start.py          # معالج البداية
│   ├── send.py           # معالج إرسال الطلبات
│   ├── status.py         # معالج حالة الطلبات
│   ├── tiers.py          # معالج الفئات
│   ├── contacts.py       # معالج جهات الاتصال
│   ├── utils.py          # دوال مساعدة
│   └── api_utils.py      # دوال الاتصال بالـ API
└── .env                   # متغيرات البيئة
```

## ملاحظات

- جميع الرسائل مركزية في `config.MESSAGES`
- يستخدم البوت `ConversationHandler` للمحادثات متعددة الخطوات
- معالجة شاملة للأخطاء مع رسائل واضحة للمستخدم
- دعم رسائل طويلة (تقسيم تلقائي)
